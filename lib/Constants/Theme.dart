import 'package:flutter/material.dart';

class AppColors
{
    static const main = Color(0xFF1864E0);
    static const background = Color(0xFFECECFC);
    static const card = Color(0xFFFFFFFF);

    static const darkText = Color(0xFF4B4B77);
    static const lightText = Color(0xFF82828F);
}